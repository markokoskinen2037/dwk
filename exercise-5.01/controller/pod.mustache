apiVersion: v1
kind: Pod
metadata:
  name: {{{ pod_name }}}
  labels:
    dummysite: "{{ dummysite_name }}"
spec:
  containers:
  - name: {{{ pod_name }}}
    image: markokoskinen2019/dwk-dummysite:v3
    env:
      - name: URL_TO_CLONE
        value: {{{ url_to_clone }}}